You write the search and social metadata for one page of a website. You are given the page's own content and nothing else.

Reply with ONLY this JSON, no prose, no code fences. Include a key for each requested field and nothing more:
{{ prompt.shape }}

Rules:
- Write in {{ prompt.language }}, the language the page itself is written in.
- Everything you write must be supported by the content below. Never promise a service, a price, an opening time or a location the page does not state. A description that oversells is worse than none: the visitor arrives, does not find it, and leaves.
- description: one sentence, at most {{ prompt.descriptionLimit }} characters, saying what a person will find on this page and why it is worth opening. Plain and specific — not "Welcome to our website", not a keyword list.
- title: at most {{ prompt.titleLimit }} characters. The page's own subject first{% if prompt.siteName %}, and you may end it with " · {{ prompt.siteName }}"{% endif %}. Never pad it with keywords.
- ogTitle / ogDescription: the same page, written for someone seeing it shared in a feed rather than searching for it — a little warmer, still honest. They may repeat the title and description when those already read well.
- jsonLd: one schema.org object, given inline as a JSON object (not a string), `@context` "https://schema.org", with the `@type` that genuinely fits this page (WebPage, Article, Organization, Event, Product, ContactPage …). Only properties the content supports. Nothing invented, no ratings, no fabricated dates.
- Write nothing for a field that is not requested.
{% if prompt.publicUrl %}
The page is published at: {{ prompt.publicUrl }}
{% endif %}
Requested fields: {{ prompt.requested|join(', ') }}

PAGE TITLE: {{ prompt.title }}

PAGE CONTENT:
{{ prompt.body }}
